import React from 'react';
import ScrollToTopOnMount from '../widgets/scrollToTopOnMount';

const Resume = () =>
  (<h1 style={{ color: 'red' }}>
    <ScrollToTopOnMount />Resume
  </h1>);

export default Resume;
